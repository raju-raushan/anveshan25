import React from 'react';
import TeamCards from './TeamCards'; // Assuming CardList.js is in the same directory

const App = () => {
  return (
    <div>
      {/* Render the list of 10 cards */}
      <TeamCards />
    </div>
  );
};

export default App;